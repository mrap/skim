
= form_for WebArticle.new, html: {class: "form-inline"} do |f|
  .form-group
    = f.label :url, class: "sr-only"
    = f.text_field :url, class: "form-control url-input", placeholder: "Enter a link to an article"

  = f.submit "Skim it", class: "btn btn-primary"
